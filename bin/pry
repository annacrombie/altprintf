#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(__dir__, '../gem/lib'))
require 'pry'
require 'altprintf'

include Altprintf
puts "Altprintf version #{Altprintf::VERSION.join('.')} loaded"

Pry.start
