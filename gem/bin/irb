#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(__dir__, '../lib'))

require 'irb'
require 'altprintf'

include Altprintf

puts "Altprintf v#{VERSION} (libaltprintf v#{LIB_VERSION}) loaded"

IRB.start
